describe("addDisplayTextDetails", () => {
  test("correct display text when displayType is null", () => {});

  test("correct display text when displayType is already defined", () => {});

  test("correct formatting for completed_by", () => {});

  test("correct formatting for last_updated", () => {});
});
